version: "3.8"

services:
  php-calculator:
    build: .
    container_name: php-calculator
    ports:
      - "8080:80"          # Default port
    volumes:
      - ./src:/var/www/html
    restart: unless-stopped
